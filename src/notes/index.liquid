---
title: Notes
layout: layouts/list.liquid
css: listPage.css
---
<section class="twocol">
  <h1>Notes</h1>
  <p>A collection of notes, quick thoughts, and personal reflections that have been bouncing around in my mind.</p>
</section>


<section class="twocol">
  {% assign postslist = collections.note | reverse %}
  <ul>
    {% for post in postslist %}
      <li>
        <h2>
          <a href="{{ post.url }}">{{ post.data.title }}</a>
        </h2>
        <small>
          <time class="postlist-date" datetime="{{ post.date | date_to_rfc822 }}">{{ post.date | date_to_long_string }}</time>
        </small>
      </li>
    {% endfor %}
  </ul>
</section>