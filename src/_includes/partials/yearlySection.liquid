<section id="{{ sectionId }}">
    <div class="splitview">
        <div class="lead-box flow">
            <div class="flow">
                <h2>{{ sectionTitle }}</h2>
                <p>{{ sectionIntro }}</p>
            </div>
            {{  sectionExtra }}
        </div>
        {{ sectionContent }}
    </div>
</section>
